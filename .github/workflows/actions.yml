name: "Test Suite"

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Testing
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: 1.69.0
